{% extends "base.jinja2" %}

{# PRE-HEADER#}
{% block pre_header %}
    <link rel="stylesheet"
          href='{{ request.static_url("stalker_pyramid:static/ace/css/fullcalendar.css") }}'/>
    <link rel="stylesheet"
          href='{{ request.static_url("stalker_pyramid:static/ace/css/bootstrap-timepicker.css") }}'/>
    <link rel="stylesheet"
          href='{{ request.static_url("stalker_pyramid:static/ace/css/datepicker.css") }}'/>
    <link rel="stylesheet"
          href='{{ request.static_url("stalker_pyramid:static/ace/css/daterangepicker.css") }}'/>

    {% include 'charts/chart_preheader.jinja2' %}

{% endblock pre_header %}

{#SIDEBAR#}
{% block sidebar %}
    {% include 'sidebar.jinja2' %}
{% endblock sidebar %}

{#BREADCRUMBS#}
{% block breadcrumbs %}
    {% set page_title='Dashboard' %}
    {% include 'breadcrumb/breadcrumbs.jinja2' %}
{% endblock breadcrumbs %}

{#PAGE-CONTENT#}
{% block page_content %}

    {% set page_title='Dashboard' %}

    {% include 'page_header.jinja2' %}
    <!--/.page-header-->
    <div class="row-fluid">
        <div class="span12">
            <!--PAGE CONTENT BEGINS-->
            <div class="row-fluid">
                <div class="span6">
                    <div id="calendar"></div>
                </div>
                <div class="span6">
                    <div class="row-fluid">
                        <select id='project_cb' class='span6 input-block-level'>
                        </select>
                    </div>
                    <div class="space"></div>
                    <div class="hr hr8 hr-double"></div>
                    <div class="space"></div>
                    <div id="infobox"
                         class="center">
                        {% include 'components/infobtn.jinja2' %}
{#                        <div class="infobox infobox-black infobox-small">#}
{#                            <div class="infobox-icon">#}
{#                                <i class="icon-bullhorn bigger-230"></i>#}
{#                            </div>#}
{#                            <div class="infobox-data">#}
{#                                <div class="infobox-data-number">#}
{#                                    <a data-target="#html_template"#}
{#                                       data-toggle="modal"#}
{#                                       data-keyboard=false#}
{#                                       href="/entities/{{ entity.id }}/tasks/filter/-1/list"#}
{#                                       class="grey">Black List</a>#}
{#                                </div>#}
{#                            </div>#}
{#                        </div>#}
                    </div>
                    <div class="space"></div>
                    <div class="hr hr8 hr-double"></div>

                    {% include 'task/list/list_entity_tasks_by_filter_.jinja2' %}
                </div>
            </div>
        </div>
    </div>
    <!--PAGE CONTENT ENDS-->

{% endblock page_content %}

{#EXTRA-SCRIPTS#}
{% block extrascripts %}


{% raw %}
<script id="tmpl_itemThead" type="text/x-dot-template">
    <tr>
        <th>Start</th>
        <th>Name</th>
        <th>Hours Based on Bid</th>
        <th>Hours to Complete</th>
        <th></th>
    </tr>
</script>
{% endraw %}


{% raw %}
<script id="tmpl_itemRow" type="text/x-dot-template">
    <tr>
        <td>{{=it.start}}</td>
        <td><a href='/tasks/{{=it.id}}/view'>{{=it.full_path}}</a></td>
        <td style="background-color: {{=it.bid_stat}}; opacity:0.8;">{{=it.hour_based_on_bid}}</td>
        <td>{{=it.hour_to_complete}}</td>
        <td>
            <div class="hidden-phone visible-desktop action-buttons">
                {{ if (it.status === 'wip' || it.status === 'hrev')  { }}
                    <a id='update_task_item'
                       data-target="#dialog_template"
                       data-toggle="modal"
                       data-keyboard=false
                       href="/tasks/{{=it.id}}/force_status/CMPL/dialog"
                       tabindex="-1">
                        <i class="icon-check bigger-120">&nbsp</i>
                    </a>
                {{ } }}

                {{ if (it.request_extra_time)  { }}
                    <a
                        class="red"
                        data-target="#dialog_template"
                        data-toggle="modal"
                        data-keyboard=false
                        data-rel="tooltip"
                        data-placement="top"
                        title="Request Extra Time"
                        href="{{=it.request_extra_time}}">
                        <i class="icon-time bigger-130"></i>
                    </a>
                {{ } }}

                {{ if (it.request_review) { }}
                    <a
                        class="green"
                        data-target="#dialog_template"
                        data-toggle="modal"
                        data-keyboard=false
                        data-rel="tooltip"
                        data-placement="top"
                        title="Request Review"
                        href="{{=it.request_review}}">
                        <i class="icon-comment-alt bigger-130"></i>
                    </a>

                {{ } }}
                {{ if (it.review) { }}
                    <a
                        class="purple"
                        data-target="#dialog_template"
                        data-toggle="modal"
                        data-keyboard=false
                        data-rel="tooltip"
                        data-placement="top"
                        title="Review"
                        href="{{=it.review }}">
                        <i class="icon-comments bigger-130"></i>
                    </a>
                {{ } }}

            </div>
        </td>
    </tr>
</script>
{% endraw %}
{% raw %}
<script id="tmpl_itemFooterRow" type="text/x-dot-template">
    <tr>
        <td>TOTAL</td>
        <td></td>
        <td></td>
        <td></td>
        <td data-sType="num-html">

        </td>
        <td><b>TOTAL:</b></td>
        <td>{{=it.total_hours}}</td>
        <td></td>
        <td></td>
    </tr>
</script>
{% endraw %}

    {% if entity.tasks | count > 0 %}
        {% if has_permission('Update_User') or logged_in_user.id == entity.id %}
            {% set event_type='TimeLog' %}
        {% endif %}
    {% endif %}


    {% include 'calendar/calendar.jinja2' %}
    {% include 'charts/pie_chart.jinja2' %}
    <script class="include" type="text/javascript"
        src='{{ request.static_url("stalker_pyramid:static/jqPlot/jquery.jqplot.min.js") }}'></script>

    <script src='{{ request.static_url("stalker_pyramid:static/ace/js/flot/jquery.flot.min.js") }}'></script>
    <script src='{{ request.static_url("stalker_pyramid:static/ace/js/flot/jquery.flot.pie.min.js") }}'></script>

    <script type="text/javascript">

     $(document).ready(function () {

            var project_cb = $('#project_cb');
            var project_id = project_cb.val();

            var status_code = '';

            {% raw %}
                var option_template = doT.template('<option value={{=it.id}}>{{=it.name}}</option>');
            {% endraw %}

            project_cb.chosen({
                search_contains: true,
                enable_split_word_search: true
            });

            $.getJSON('/entities/{{ entity.id }}/projects/').then(function(data){

                project_cb.find('option').remove();

                var data_count = data.length;

                for (var i=0; i < data_count; i++){
                    project_cb.append(option_template(data[i]));
                }

                // update the chosen fields
                project_cb.trigger('liszt:updated');
                project_id = project_cb.val();

                initInfoBtn('/entities/{{ entity.id }}/tasks_stats/?project_id='+project_id);

                project_cb.on('change', function () {
                    project_id = $(this).val();
                    initInfoBtn('/entities/{{ entity.id }}/tasks_stats/?project_id='+project_id);
{#                    init_html_modal(status_code, project_id);#}
                });
            });



            $.getJSON('/users/{{ entity.id }}/events/?keys=time_log&keys=vacation').then(function (data) {
                var events = [];

                var total_timelogs = 0;

                for (var i = 0; i < data.length; i++) {

                    var start_date = new Date(parseInt(data[i].start));
                    var end_date = new Date(parseInt(data[i].end));

                    if(data[i].entity_type == 'timelogs'){
    {#                    var now = new Date();#}
    {#                    if(now>end_date){#}
    {#                        console.log(data[i].title +' : '+ end_date)#}
    {#                    }#}
                        var timelog_hours = (end_date-start_date)/1000/60/60;
                        total_timelogs +=timelog_hours;

                    }

                    var event = {
                        eId: data[i].id,
                        entity_type: data[i].entity_type,
                        title: data[i].title,
                        start: start_date,
                        end: end_date,
                        className: data[i].className,
                        allDay: data[i].allDay
                    };

                    events.push(event);
                }

                $(document).ready(function () {
                    drawCalendar('calendar', events);

    {#                var placeholder = $('#piechart-placeholder').css({'width':'90%' , 'min-height':'150px'});#}
    {#                  var data = [#}
    {#                    { label: "You have worked",  data: total_timelogs, color: "#68BC31"},#}
    {#                    { label: "Your vacations",  data: 240.5, color: "#2091CF"},#}
    {#                    { label: "We don't know what you did",  data: 8.2, color: "#AF4E96"}#}
    {##}
    {#                  ];#}
    {##}
    {#                 drawPieChart(placeholder, data);#}
    {##}
    {#                 /**#}
    {#                 we saved the drawing function and the data to redraw with different position later when switching to RTL mode dynamically#}
    {#                 so that's not needed actually.#}
    {#                 */#}
    {#                 placeholder.data('chart', data);#}
    {#                 placeholder.data('draw', drawPieChart);#}
                });

    {#      TODO: Please use this version later on, which loads events dynamically #}
    {#            var events = {#}
    {#                url: '/users/{{ entity.id }}/events/',#}
    {#                type: 'POST',#}
    {#                data: {#}
    {#                    keys: ['time_log', 'vacation', 'task']#}
    {#                }#}
    {#            };#}
    {#            drawCalendar('calendar', events);#}

            });
     });
    </script>

{% endblock extrascripts %}
